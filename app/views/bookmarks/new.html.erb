<%# Add a dropdown association docs, make sure the tables are linked properly %>
<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.association :movie, label_method: :title, value_method: :id%>
  <%= f.input :comment %>
  <%= f.submit %>
<% end %>
